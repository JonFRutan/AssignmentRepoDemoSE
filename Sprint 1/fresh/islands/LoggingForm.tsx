import { h } from "preact";

export default function LoggingForm() {
	const handleSubmit = (event: Event) => {
		event.preventDefault();

		const log = event.target as HTMLFormElement;

		//Form Values go here.

		const pain_level = form.querySelector('[name="painLevel"]') as HTMLInputElement.value;
		const quality_of_life = form.querySelector('[name="qualityOfLife"]') as HTMLInputElement.value;
		const satisfaction = form.querySelector('[name="satisfaction"]') as HTMLInputElement.value;
		const pain_level = form.querySelector('[name="painLevel"]') as HTMLInputElement.value;
		const additional_observations = form.querySelector('[name="additionalObservations"]') as HTMLInputElement.value;
		
	};

	return (
		<form style="
		
	)
}
